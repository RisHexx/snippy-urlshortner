services:
  backend:
    image: rishex/snippy-backend:latest
    container_name: backend
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - ./.env
    networks:
      - snippy-net

  frontend:
    image: rishex/snippy-frontend:latest
    container_name: frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - snippy-net

networks:
  snippy-net:
    driver: bridge
